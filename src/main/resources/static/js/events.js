// const validLettersSelectors = document.querySelectorAll(".valid-letter");
// const includedSelector = document.querySelector("#includedLetters");
// const excludedSelector = document.querySelector("#excludedLetters");
// const alphabet = "abcdefghijklmnopqrstuvwxyząęłóżźć".split("");

// function removeDuplicates(word) {
//     var resultSet = new Set(word.toLowerCase().split());
//     return Array.from(resultSet).join("");
// }

// function onPress(event) {
//     event.target.value = removeDuplicates(event.target.value + event.key);
// }

// includedSelector.addEventListener("keypress", (event) => onPress(event));
// excludedSelector.addEventListener("keypress", (event) => onPress(event));